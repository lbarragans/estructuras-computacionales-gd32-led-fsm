# Integracion pendiente

Se requieren FreeRTOS-Kernel, port RISC-V compatible, `FreeRTOSConfig.h`, heap,
tick y cambio de contexto. Solo debe marcarse validada despues de compilar,
arrancar el scheduler y medir el patron completo en la placa.
